import { createTheme } from '@mui/material';

export const desktopNavigationTheme = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },
  },
});

export const LC_DNB = {
  logo: 'immensus',
  createNewPostButton: 'Create new post',
};
